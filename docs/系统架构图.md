# 系统架构图

## 系统总体架构

```mermaid
flowchart TD
    subgraph 用户端
        A[微信小程序]
    end
    
    subgraph 云服务层
        B[Express 云后台 API]
    end
    
    subgraph 数据层
        C[(SQLite 数据库)]
    end
    
    subgraph 医生端
        D[医生端 Web 系统]
    end
    
    A -->|HTTP/HTTPS| B
    B -->|SQL| C
    D -->|HTTP/HTTPS| B
```

## 模块说明

### 1. 微信小程序（用户端）
- 用户登录与注册（手机号/微信授权）
- 虚拟设备管理（添加、编辑、删除设备）
- 睡眠数据展示（睡眠报告、分期分析）
- 环境监测（温度、湿度、噪音曲线）
- 个性化建议与作息管理

### 2. Express 云后台 API
- 身份认证与授权管理
- 设备数据接收与存储
- 睡眠数据分析与处理
- RESTful API 接口服务
- 医生授权数据共享

### 3. SQLite 数据库
- 用户信息表（用户ID、手机号、昵称、头像）
- 设备信息表（设备ID、用户ID、设备类型、状态）
- 睡眠数据表（记录ID、用户ID、睡眠时段、分期数据）
- 环境数据表（记录ID、设备ID、温度、湿度、噪音）
- 医生授权表（授权ID、用户ID、医生ID、授权状态）

### 4. 医生端 Web 系统
- 医生登录与身份验证
- 授权用户列表管理
- 用户睡眠数据查看
- 诊疗建议记录

## 数据流向说明

```mermaid
sequenceDiagram
    participant 小程序 as 微信小程序
    participant API as Express API
    participant DB as SQLite数据库
    participant 医生Web as 医生端Web

    Note over 小程序,医生Web: 用户登录流程
    小程序->>API: POST /api/login (手机号+验证码)
    API->>DB: 查询用户信息
    DB-->>API: 返回用户记录
    API-->>小程序: 返回登录令牌(token)

    Note over 小程序,医生Web: 设备数据上传流程
    小程序->>API: POST /api/device/data (设备ID+传感器数据)
    API->>DB: 插入环境数据记录
    DB-->>API: 返回插入结果
    API-->>小程序: 返回成功响应

    Note over 小程序,医生Web: 睡眠报告生成流程
    小程序->>API: GET /api/sleep/report (用户ID+日期)
    API->>DB: 查询睡眠数据
    DB-->>API: 返回睡眠记录
    API->>API: 分析睡眠分期
    API-->>小程序: 返回睡眠报告

    Note over 小程序,医生Web: 医生查看数据流程
    医生Web->>API: GET /api/doctor/patients (医生ID)
    API->>DB: 查询授权用户列表
    DB-->>API: 返回用户列表
    API-->>医生Web: 返回授权用户

    医生Web->>API: GET /api/doctor/patient/sleep (用户ID)
    API->>DB: 查询用户睡眠数据
    DB-->>API: 返回睡眠记录
    API-->>医生Web: 返回患者睡眠数据
```

## 架构特点

1. **分层架构**：清晰的三层架构（表现层、服务层、数据层），职责分离
2. **松耦合**：各模块通过API接口交互，便于独立开发和部署
3. **安全性**：基于令牌的身份认证，支持医生授权机制
4. **轻量级**：使用SQLite数据库，适合轻量级部署场景
5. **扩展性**：RESTful API设计，便于后续扩展新功能模块
